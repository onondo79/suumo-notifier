# Gitに上げたくないファイルをここに書きます。
# ここに書いたファイルは、GitHubにアップロードされません。

# パスワードを書いた .env は絶対に上げない（最重要）
.env

# Pythonが自動で作る一時ファイル
__pycache__/
*.pyc

# OSが作る不要ファイル
.DS_Store
Thumbs.db

# 注意: data/ フォルダは「上げる」ので、ここには書きません。
#       （新着判定の記録を次回も使うため、リポジトリに保存します）
